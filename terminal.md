# 전체 흐름 미리 보기

1단계: 터미널로 작업 폴더 만들기
2단계: Docker 설치하고 제대로 깔렸는지 확인
3단계: 컨테이너 한번 실행해보기 (연습)
4단계: 나만의 웹 서버를 Dockerfile로 만들기
5단계: 포트 연결해서 브라우저로 접속 확인
6단계: 파일 수정하면 바로 반영되게 하기 (바인드 마운트)
7단계: 컨테이너 지워도 데이터 안 날아가게 하기 (볼륨)

## 터미널, 도커, 깃/깃허브란?

> 터미널 : 마우스 없이 글자만으로 컴퓨터에게 명령을 내리는 창

평소에 컴퓨터를 쓸 때는 아이콘을 더블클릭하고, 마우스로 폴더를 열고, 버튼을 누르죠. 이런 방식을 "그래픽 인터페이스(GUI)"라고 해요.

터미널은 그 반대예요. 까만 화면에 글자를 입력해서 컴퓨터에게 직접 지시를 내려요.

예를 들면:

cd 문서 → "문서 폴더로 이동해줘"
ls (또는 dir) → "지금 폴더에 뭐가 있는지 보여줘"
mkdir 새폴더 → "새폴더라는 이름의 폴더를 만들어줘"

왜 쓰나요?

마우스로 100번 클릭할 걸 명령어 한 줄로 끝낼 수 있어요
복잡한 작업(파일 수백 개 한꺼번에 처리 등)을 자동화하기 편해요
개발 도구들이 대부분 터미널 명령어로 작동해요 (뒤에 나올 Docker, Git도 마찬가지)

비유하자면, GUI가 "레스토랑에서 메뉴판 보고 주문하기"라면, 터미널은 "주방에 직접 들어가서 요리사에게 정확한 레시피를 말해주기" 같은 느낌이에요.

> 도커 : 프로그램을 "어디서든 똑같이 작동하는 상자"에 포장하는 기술

프로그래머들이 자주 겪는 문제가 있어요: "내 컴퓨터에서는 잘 되는데, 다른 사람 컴퓨터에서는 안 돼요!" 이유는 컴퓨터마다 설치된 프로그램 버전, 환경 설정이 다 다르기 때문이에요.

Docker는 이 문제를 해결해요. 프로그램과 그게 실행되는 데 필요한 모든 것(설정, 라이브러리 등)을 컨테이너라는 상자 하나에 통째로 넣어버려요.

이 컨테이너는:

내 컴퓨터에서 실행하든
동료 컴퓨터에서 실행하든
회사 서버에서 실행하든

항상 똑같이 작동해요. 마치 이사할 때 짐을 다 컨테이너 박스에 넣어서 어디로 옮기든 내용물이 그대로인 것처럼요.

비유: 도시락을 생각해보세요. 집에서 먹든, 회사에서 먹든, 여행 가서 먹든 도시락 안의 반찬은 똑같잖아요? Docker 컨테이너가 그 도시락 같은 역할이에요.

> 깃/깃허브 : 코드를 저장하고, 여러 사람이 함께 작업할 수 있게 해주는 온라인 창고

GitHub를 이해하려면 먼저 Git을 알아야 해요.

Git: 코드가 바뀔 때마다 "버전"을 기록해주는 프로그램. 문서 작업할 때 "저장1, 저장2, 최종본, 진짜최종본" 이렇게 파일을 여러 개 만드는 대신, Git은 변경 이력을 깔끔하게 관리해줘요. 언제든 예전 버전으로 되돌릴 수도 있고요.
GitHub: 그 Git으로 관리하는 코드를 인터넷에 올려두는 사이트. 구글 드라이브가 문서를 클라우드에 저장하듯, GitHub는 코드를 클라우드에 저장해요.

왜 유용한가요?

여러 사람이 같은 프로젝트를 동시에 작업해도 코드가 꼬이지 않게 관리돼요
내가 실수해도 예전 버전으로 되돌릴 수 있어요
전 세계 개발자들이 자기가 만든 오픈소스 코드를 공유하는 공간이기도 해요 (마치 SNS처럼요)

> 셋(터미널, 도커, 깃/깃허브)을 같이 쓰는 이유 

1. GitHub에서 동료가 만든 코드를 다운로드
2. 터미널을 열어서 그 코드를 실행하는 명령어를 입력
3. 근데 내 컴퓨터랑 환경이 달라서 에러가 남 → Docker로 포장된 버전을 받아서 실행하면 문제없이 똑같이 작동

## 1단계: 터미널 열고 작업 폴더 만들기
```bash
# 지금 내가 어디 있는지 확인
pwd

# 바탕화면으로 이동
cd ~/Desktop

# 미션용 폴더 만들기
mkdir docker-mission

# 그 폴더로 들어가기
cd docker-mission

# 지금 위치 다시 확인 (docker-mission이 보여야 함)
pwd
```

## 추가내용 : 권한(permission) 관련해서 미션에서 요구

```bash
# 파일 실행 권한 주기 (나중에 스크립트 파일 만들면 씀)
chmod 000(숫자) 파일이름

# 현재 폴더 안의 파일들 권한 자세히 보기
ls -al
```

## 2단계: Docker 설치하기

```bash
docker --version
```
>> Docker 28.5.2 ver 사용 중임을 확인.

## 3단계: 컨테이너 한번 실행해보기 (연습용)

```bash
docker run hello-world
```

이 명령어가 하는 일:

1. 컴퓨터에 hello-world라는 이미지가 있는지 확인
2. 없으면 인터넷에서 다운로드 (이걸 pull이라고 해요)
3. 그 이미지로 컨테이너를 하나 만들어서 실행

>> "Hello from Docker!" 메세지 뜨는 것 확인.

https://claude.ai/api/da9e02e1-765c-4088-a914-db6c19cb0c21/files/0728f386-e7ed-4a08-bc17-85e51b63974d/preview

>> 이미지(image)와 컨테이너(container) 개념 차이

이미지: 요리 레시피 (설계도)
컨테이너: 그 레시피로 실제 만든 요리 (실행 중인 것)

같은 이미지로 컨테이너를 여러 개 만들 수 있어요. 붕어빵 틀(이미지)이 하나 있으면 붕어빵(컨테이너)을 여러 개 구울 수 있는 것처럼요.

지금 떠있는 컨테이너 확인하기:
```bash
# 현재 실행 중인 컨테이너
docker ps

# 실행 중이었던 것 포함 전부 보기 (hello-world는 실행 끝나면 바로 종료됨)
docker ps -a

# 다운받은 이미지 목록
docker images
```

## 4단계: 간단한 웹 서버 만들기 (Dockerfile)

폴더 안에 파일 2개를 만들 거예요:
```bash
# 지금 docker-mission 폴더 안에 있는지 확인
pwd
```

① index.html 파일 만들기 (내 웹서버가 보여줄 내용
```bash
echo "<h1>내 첫 Docker 웹서버</h1>" > index.html
```

② Dockerfile 만들기 (이미지 설계도)
```bash
touch Dockerfile
```

위 도커파일을 VS CODE로 열어서 텍스트 기입.

```dockerfile
FROM nginx:alpine
COPY index.html /usr/share/nginx/html/index.html
```

이게 무슨 뜻이냐면:

FROM nginx:alpine → "nginx(웹서버 프로그램)가 이미 깔려있는 기본 이미지를 가져다 써"

COPY index.html ... → "내가 만든 index.html을 컨테이너 안의 저 경로에 복사해 넣어"

③ 이미지 빌드하기
```bash
docker build -t my-web .
```

-t my-web → 이미지 이름을 "my-web"으로 짓겠다는 뜻
맨 뒤 . → "지금 이 폴더에 있는 Dockerfile로"

빌드 끝나고 확인:
```bash
docker images
```

>> my-web이 목록에 뜨는 것 확인.

## 5단계: 포트 연결해서 브라우저로 확인

```bash
docker run -d -p 8080:80 --name my-web-container my-web
```

-d → 백그라운드에서 조용히 실행 (터미널 안 막힘)

-p 8080:80 → 포트 매핑. "내 컴퓨터의 8080번 포트로 들어오는 요청을, 컨테이너 안의 80번 포트(nginx가 듣고 있는 곳)로 연결해줘"

--name my-web-container → 컨테이너 이름 지정

my-web → 아까 만든 이미지 이름

>> 포트매핑이 필요한 이유

컨테이너는 내 컴퓨터랑 격리된 별도의 작은 세상이에요. 그 안에서 웹서버가 80번 포트로 열려있어도, 밖(내 컴퓨터)에서는 그 문을 직접 볼 수가 없어요. 그래서 "내 컴퓨터의 몇 번 문이랑 컨테이너의 몇 번 문을 연결할지" 지정해줘야 해요.

이제 브라우저 열고 주소창에: 
http://localhost:8080

결과 : 처음엔 글자가 깨져서 나타났지만, 

https://claude.ai/api/da9e02e1-765c-4088-a914-db6c19cb0c21/files/4d2ad111-1329-49eb-a2b5-435e9ae4a9bd/preview

수정 후 제대로 보임.

>> 고치는 방법 : index.html 파일에 "이 파일은 UTF-8이야"라는 태그 한 줄을 추가

VS Code로 열어볼게요:
```bash
code index.html
```

기존 내용을 전체 지우고 (Command+A → Delete), 아래 내용으로 통째로 바꿔주세요:
```bash
<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
</head>
<body>
  <h1>내 첫 Docker 웹서버</h1>
</body>
</html>
```

<meta charset="UTF-8"> 이 한 줄이 핵심이에요. "이 문서는 UTF-8 방식으로 한글이 저장되어 있다"고 브라우저에게 알려주는 태그예요.

저장 후 터미널로 돌아와서 :
```bash
cat index.html
```

입력 후 컨테이너 재시작 :
```bash
docker stop my-web-container
docker rm my-web-container
docker build -t my-web .
docker run -d -p 8080:80 --name my-web-container my-web
```

1. stop → 지금 실행 중인 컨테이너 멈추기
2. rm → 그 컨테이너 삭제 (이미지는 안 지워짐, 컨테이너만)
3. build → 수정된 index.html로 이미지 새로 만들기
4. run → 새 이미지로 컨테이너 다시 실행

다 실행한 후 브라우저에서 localhost:8080 새로고침 -> 한글 제대로 나오는 것 확인!

https://claude.ai/api/da9e02e1-765c-4088-a914-db6c19cb0c21/files/078568d4-9678-4c53-ae41-30d67fee04f2/preview

추가 : (컨테이너 상태 확인/관리 명령어)
```bash
docker ps                          # 실행 중인 컨테이너 확인
docker logs my-web-container       # 로그(기록) 보기
docker stop my-web-container       # 멈추기
docker start my-web-container      # 다시 시작
docker rm my-web-container         # 완전히 삭제 (멈춘 상태에서만 가능)
```

## 6단계: 파일 수정하면 바로 반영되게 하기 (바인드 마운트)

지금 방식은 index.html을 이미지에 "복사"해 넣은 거라서, index.html을 수정해도 컨테이너에는 반영이 안 돼요. 다시 빌드해야 하죠. 이게 번거로우니까 바인드 마운트를 써볼게요.

일단 기존 컨테이너 정리:
```bash
docker stop my-web-container
docker rm my-web-container
```

이번엔 이렇게 실행:
```bash
docker run -d -p 8080:80 --name my-web-container -v $(pwd)/index.html:/usr/share/nginx/html/index.html my-web
```

$(pwd)/index.html → 내 컴퓨터(호스트)에 있는 실제 파일 경로

/usr/share/nginx/html/index.html → 컨테이너 안의 경로

이제 테스트: 터미널에서 index.html 내용을 바꿔보세요.
```bash
echo "<h1>수정했어요! 바로 반영되나요?</h1>" > index.html
```

결과 : 브라우저에서 localhost:8080 새로고침 → 재시작 없이 바로 바뀐 내용이 보임

왜 이게 되냐면: 파일을 컨테이너 안에 "복사"한 게 아니라, 내 컴퓨터의 실제 파일을 컨테이너가 "직접 들여다보게" 연결해놓은 거라서예요. 원본이 바뀌면 컨테이너에서 보는 것도 바로 바뀌어요.

## 7단계: 데이터 영속성 확인 (볼륨)

핵심 : "컨테이너를 지워도 데이터는 안 사라지게" 하기

```bash
# 볼륨 만들기
docker volume create my-data

# 볼륨을 마운트해서 컨테이너 실행 (ubuntu 이미지로 간단 테스트)
docker run -it --name test-container -v my-data:/data ubuntu bash
```
컨테이너 안(bash 프롬프트 안)에서:
```bash
echo "이 데이터는 살아남을까?" > /data/test.txt
exit
```

이제 컨테이너를 완전히 삭제해보세요:
```bash
docker rm test-container
```

컨테이너는 삭제됐지만, 볼륨은 살아있어요. 새 컨테이너를 만들어서 같은 볼륨을 연결해보면:
```bash
docker run -it --name test-container2 -v my-data:/data ubuntu bash
```

컨테이너 안에서 :
```bash
cat /data/test.txt
```

결과 다 확인한 다음 빠져나오기 :
```bash
exit
```

## 추가내용 (과제 3)

1. 절대 경로 vs 상대 경로

먼저 경로 이야기부터 시작하겠습니다.

> 절대경로 : 최상위 폴더(루트)부터 시작해 목적지까지의 전체 경로를 적는 방식 / 어디서 실행하든 결과가 같지만 주소가 길어진다

> 상대경로 : 내가 지금 있는 곳을 기준으로 목적지의 상태를 적는 방식 / 주소가 짧고 유연하지만 현재 내 위치에 따라 결과가 바뀔 수 있다

2. 파일 권한의 의미(r/w/x)와 해석

> 읽기 (READ) : 파일 내용을 보거나 폴더 안의 목록을 확인할 수 있음 / "4" 로 표현

> 쓰기 (Write) : 파일 내용을 수정, 저장하거나 지울 수 있고, 폴더 안에서 새 파일을 만들 수 있음 / "2"로 표현

> 실행 (Execute) : 프로그램이나 스크립트 파일을 실행할 수 있음 (폴더의 경우 내부 파일에 접근할 때 필요) / "1"로 표현

* 권한 대상의 주체

> 소유자 (User) : 파일을 만든 사람

> 그룹 (Group) : 그 사람이 속한 특정 단체나 부서 사람들

> 기타 사용자 (Others) : 소유자도 아니고 그룹에도 속하지 않은 외부인

* 표기 방법과 의미

> 문자 표기 : 세 주체가 순서대로 rwx 권한을 가졌는지 표시 -> 없으면 - 로 나옴
> 숫자 표기의 점수를 더해서 표시 -> 예, 7 = 4 + 2 + 1 (모든 권한을 가진다는 뜻)

3. 포트 매핑이 필요한 이유 : 외부에서 들어오는 도커 컨테이너의 특정 프로그램을 정확히 전달하기 위해

4. Docker 볼륨 (영속 데이터) 란?
> 도커 컨테이너가 생성하고 사용하는 데이터를 영구적으로 안전하게 보관하기 위해, 호스트 컴퓨터의 저장 공간과 컨테이너 내부를 연결(마운트)해주는 기능

## 심화내용

“호스트 포트가 이미 사용 중”이라 포트 매핑이 실패한다면, 어떤 순서로 원인을 진단할지 설명할 수 있는가?

 컨테이너 삭제 후 데이터가 사라진 경험이 있다면, 이를 방지하기 위한 대안을 설명할 수 있는가?

 이 미션에서 가장 어려웠던 지점과, 해결 과정(가설 → 확인 → 조치)을 근거와 함께 설명할 수 있는가?

 